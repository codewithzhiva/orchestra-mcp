{
  "name": "orchestra-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the Local AI Ecosystem — Orchestra, RAG Chat, LLM Gateway, and Observatory as MCP tools.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "orchestra-mcp": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
